# ============================================================
# Frontend (client-safe — prefix VITE_ is exposed to the browser)
# Copy this file to .env and fill in the values.
# ============================================================
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=
VITE_APPWRITE_DATABASE_ID=
VITE_APPWRITE_PROJECT_NAME =
VITE_APPWRITE_RESUME_BUCKET_ID=
# Free plan allows one bucket — reuse the resume bucket (which also accepts images).
# If you upgrade, set this to its own bucket id (e.g. "avatars") and run setup:appwrite.
VITE_APPWRITE_AVATAR_BUCKET_ID=resumes

# Node.js backend URL (used by the frontend for business-logic APIs)
VITE_API_BASE_URL=http://localhost:5000

# ============================================================
# Node.js Backend (server/.env — NEVER expose these to the browser)
# ============================================================
PORT=5000

APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
APPWRITE_PROJECT_ID=
APPWRITE_API_KEY=
APPWRITE_DATABASE_ID=

# Python AI service
AI_SERVICE_URL=http://localhost:8000

# Email service — SendGrid HTTPS (preferred on Render) or Nodemailer SMTP for local dev
SENDGRID_API_KEY=your_sendgrid_api_key
SENDGRID_SENDER=skillsaarthi <skillsaarthi.support@gmail.com>
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
FRONTEND_URL=http://localhost:5173

# Auth / OTP settings
PENDING_EXPIRY_MS=600000          # 10 minutes for pending OTP
VERIFICATION_TOKEN_EXPIRY_MS=86400000  # 24h for legacy email verification links
RESET_TOKEN_EXPIRY_MS=3600000     # 1h for password reset
RESEND_COOLDOWN_MS=60000          # 60s cooldown for resend OTP
OTP_LENGTH=6
MAX_OTP_ATTEMPTS=5
PENDING_ENCRYPTION_KEY=           # optional: 32-byte hex; defaults to hash of APPWRITE_API_KEY + PROJECT_ID

# External services
GITHUB_TOKEN=
LLM_API_KEY=

# Comma-separated emails allowed to use the admin routes
# (e.g. ADMIN_EMAILS=owner@example.com,admin@example.com). Empty = admin API disabled.
ADMIN_EMAILS=

# ============================================================
# Scripts  (scripts/.env.setup)
# ============================================================
# Internship importer (scripts/import-internships.mjs)
SOURCE=file             # file (JSON feed) or remotive (API)
FEED_FILE=              # path to the JSON feed (default: scripts/feeds/internships.json)
INTERNSHIP_TTL_DAYS=30  # how long imported listings stay visible before auto-expiring
IMPORT_MAX=50           # max openings imported per run

# ============================================================
# Python AI Service (ai-service/.env)
# ============================================================
PORT=8000

# TCET CoE AI Gateway (OpenAI-compatible)
AI_BASE_URL=https://ai.tcetcercd.in/v1
AI_MODEL=Qwen3.6-35B-A3B
AI_KEY=
LLM_API_KEY=
